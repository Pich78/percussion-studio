/* 
  js/config.js
  Configuration settings for the application
*/

export const config = {
    // Set to false for production (GitHub Pages) to enable browser caching
    // Set to true for development to always fetch fresh files
    isDevelopment: false,

    // When true, adds detailed console logging
    verboseLogging: false
};
