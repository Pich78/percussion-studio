{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["bash", "-lc", "source \"$HOME/.nvm/nvm.sh\" && exec npx -y @playwright/mcp@latest --browser chromium --headless"],
      "enabled": true
    }
  },
  "experimental": {
    "mcp_timeout": 120000
  }
}
